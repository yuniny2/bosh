worker_processes 1;
daemon off;

error_log <%= error_log_path %> debug;

events { }

stream {
  server {
    listen     <%= nginx_port %>;
    proxy_pass <%= database_host %>:<%= database_port %>;
  }
}
